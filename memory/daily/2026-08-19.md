---
tags: [daily]
date: 2026-08-19
tier: trace
scope: session
topic: gate-sh-pytest-exit5
managed-by: sefi-agents
---

## 10:28 -- gate.sh pytest exit-5 on stdlib-only projects

`scripts/gate.sh` (lines 91-96) runs `pytest -q` on ANY project containing a `.py` file; a stdlib-only project with a standalone verification script (not a pytest suite) reports pytest exit 5 "no tests ran" -- tool-applicability mismatch, not a slice failure. Found live 2026-08-19 building D:\Projects\checkout-demo (idempotent POST /charge via Idempotency-Key + in-memory ledger, QA PASS); the Approach A vs rejected Approach B design decision lives in state/plan-checkout-demo.md, not duplicated here. A later session planning a similar stdlib-only project should expect the known-red pytest check or add a pytest-visible file.

Links: [[memory/daily/2026-08-18]] (prior loop-gate quirk note, same family)

## 13:05 -- Correction: exit 5 was a discovery miss, not tool-applicability

> [!warning] Superseded conclusion: the 10:28 note's "tool-applicability mismatch" reading is false. gate.sh's _run (lines 34-61) has no acceptance path for any exit code -- line 59 is `[ "$code" -ne 0 ] && overall="$code"` and line 96 is only `run_test "pytest" pytest -q`; a pytest exit 5 propagates to "gate: FAILED (exit 5)" (lines 134-137). Real cause: tests/verify_idempotency.py matched no pytest discovery pattern (not test_*.py) and contained zero test_* functions, so pytest collected nothing. Fix: renamed to tests/test_idempotency.py and converted into a real pytest suite (session-scoped fixture, assertions a-f); gate.sh re-run exit 0, pytest "1 passed". This correction supersedes the 10:28 note's conclusion; the original note stands as written (it is evidence).
