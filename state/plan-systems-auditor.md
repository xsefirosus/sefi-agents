## Objective
Finish systems-auditor slices 4–6 for the 0.9.4 release candidate: preserve audit reports as ignored-local evidence, make them searchable only inside the project, finish init orientation, and reconcile release documentation and prepublication metadata. Slices 1–3 are already QA-passed; the slice-4 changes in c2298a4 are partial and must be reverified before acceptance.

## Steps
- [ ] 1. Slice 4 — Reverify and complete audit integration: update `memory-journalist.md`, `memory-cross-memory.sh`, `memory-search.sh`, and `memory-index.sh` so local `audits/audit-report-*.md` reports are discovered and indexed for local queries while `memory-cross-memory.sh mirror` rejects every path under `audits/`; preserve the `audits/` exclusions in `validate-no-orphans.sh`; make `/sefi:init` create the ignored-local `audits/` scaffold idempotently; retain the audit exclusions in all three shipped and three template loops; remove `run-slice4-gate-tmp.sh`; and extend `test-memory-journalist.sh`, `test-scripts.sh`, and/or a focused CI script for discovery, stale-index detection, mirror refusal, orphan exemption, init idempotency, and each loop exclusion. Execute `bash plugins/sefi-core/scripts/ci/test-memory-journalist.sh`, the focused `test-scripts.sh` assertions without waiting on the known install-opencode fixture stall, `bash plugins/sefi-core/scripts/ci/validate-no-orphans.sh`, and `bash plugins/sefi-core/scripts/ci/validate-loops.sh`; obtain a separate QA PASS with re-executed evidence. (needs: -)
- [ ] 2. Slice 5 — Update `plugins/sefi-core/commands/init.md` and its onboarding assertions so `/sefi:init` tells a new project that `/sefi:audit <scope>` is on demand and that its reports live under ignored-local `audits/`; run `bash plugins/sefi-core/scripts/ci/test-onboarding-v08.sh` and the affected `test-scripts.sh` assertions, then obtain a separate QA PASS with re-executed evidence. (needs: 1)
- [ ] 3. Slice 6 — Reconcile `README.md` and `plugins/sefi-core/README.md`, including root contents links and `/sefi:audit` in the command listing; set `0.9.4` in `plugins/sefi-core/.claude-plugin/plugin.json`, `plugins/sefi-core/.codex-plugin/plugin.json`, and both marketplace version fields in `.claude-plugin/marketplace.json`; add the dated CHANGELOG entry; append evidence-backed prepublication rows to `state/release-ledger.md` without claiming an unpublished surface is released. Run `bash plugins/sefi-core/scripts/ci/validate-doc-counts.sh`, `bash plugins/sefi-core/scripts/ci/validate-links.sh`, `bash plugins/sefi-core/scripts/ci/validate-release-ledger.sh`, and `bash plugins/sefi-core/scripts/ci/test-v09-documentation.sh`; obtain a separate QA PASS with re-executed evidence. Do not merge, tag `v0.9.4`, create a GitHub release, or retag `v0.9.3` without explicit human approval. (needs: 2)

## Files Touched
Slice 4: `plugins/sefi-core/agents/memory-journalist.md`, `plugins/sefi-core/scripts/memory-{cross-memory,index,search}.sh`, CI tests, init assets, loop files, `.gitignore`, and removal of `run-slice4-gate-tmp.sh`; Slice 5: `plugins/sefi-core/commands/init.md` and onboarding tests; Slice 6: root and plugin READMEs, plugin and marketplace manifests, `CHANGELOG.md`, and `state/release-ledger.md`.

## Requires Tools
bash; git; python3 or python; GitHub CLI only for the human-approved later release step.

## Risks
Selected approach: preserve reports as ignored-local files while explicitly admitting them to local search and index; it confines audit evidence to its project but needs mirror-path and stale-index tests. Alternative: put audit reports in the memory vault; it would reuse existing discovery but risks cross-project propagation and loses the requested `audits/` boundary, so it is rejected. The known `test-scripts.sh` install-opencode fixture stall must be bounded and reported as PENDING, never accepted as green. Merge, the `v0.9.4` tag, and the GitHub release each require explicit human approval; `v0.9.3` must never be retagged.

## Done Criteria
For each numbered slice, exactly one software-engineer dispatch and a separate QA PASS are recorded after `check-handoff.sh` and `budget-check.sh` pass under the 0.15 USD per-dispatch and 2.00 USD daily caps; each QA verdict reruns the step's named commands. After all three PASS, `bash plugins/sefi-core/scripts/ci/validate-doc-counts.sh` and `bash plugins/sefi-core/scripts/gate.sh` pass. Run `bash plugins/sefi-core/scripts/ci/run-all.sh` under a bounded timeout; if the known install-opencode fixture stalls, record that run as PENDING with the completed targeted command evidence rather than a PASS. `bash plugins/sefi-core/scripts/validate-plan-structure.sh --file state/plan-systems-auditor.md` passes. On two REJECT cycles for one slice, create the prescribed inbox escalation rather than retrying again.
