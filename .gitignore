# Personal-use planning/build artifacts -- kept locally, never tracked or shipped.
docs/archive/
docs/superpowers/
docs/master-prompt-FINAL-v4.md
docs/master-prompt-v4-provenance.md

# Loop worktrees (docs/LOOPS.md worktree procedure) -- project-local, never global.
.worktrees/

# /sefi:init's harness marker -- a machine-local install fact (which harness ran init),
# never a vault note or a state ledger entry.
.sefi/

# Benchmark run artifacts (benchmarks/README.md: blinded paired A/B harness). Raw trial
# output, worktrees, and judge transcripts -- machine-local evidence with a leak boundary,
# never committed. The design, cases, fixtures, and scorer under benchmarks/ ARE tracked;
# a promoted scorecard is committed to benchmarks/published/ (NOT ignored).
benchmarks/results/

# Python bytecode and tooling scratch from running benchmarks/scorecard.py and its test
# (dev-only tooling). __pycache__/*.pyc embeds an absolute build path -- never shipped,
# never committed.
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
